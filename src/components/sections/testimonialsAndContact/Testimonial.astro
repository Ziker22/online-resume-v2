---
import { Image } from 'astro:assets';
import quoteIcon from '../../../images/quote.svg';

type Props = {
  text: string;
  author: string;
  position: string;
};

const { text, author, position } = Astro.props;
---

<div class="flex flex-col px-4 md:px-8">
  <div class="flex w-full items-start py-4">
    <Image src={quoteIcon} alt="Quote icon" class="size-14" />
  </div>
  <p class="mb-6 text-base">
    {text}
  </p>
  <p class="text-lg font-semibold">— {author}</p>
  <p class="pb-4 font-semibold">{position}</p>
</div>

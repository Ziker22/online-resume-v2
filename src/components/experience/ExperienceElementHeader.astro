---
import type { TExperience } from '../../types/TExperience'

type Props = {
    data: TExperience
}

const {data} = Astro.props
---

<div class="sticky top-0 z-1 bg-background shadow-background shadow-xl">
    <div class="flex justify-between py-4">

        <h1 class="text-2xl font-bold text-center px-4">{data.company.name} </h1>
    </div>
</div>